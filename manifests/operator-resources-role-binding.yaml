apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: operator-resources-role-binding
  labels:
    app.kubernetes.io/part-of: adoption-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: operator-resources-role
subjects:
- kind: ServiceAccount
  name: adoption-operator
